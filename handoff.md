# 成员每日任务与完成情况

最后更新：2026-09-02

本文件是三名成员查看当前进度和下一步任务的唯一入口。每次开始开发前先同步 `main`，再阅读本文件；长期协作规则见 `CONTRIBUTING.md`。

## 当前状态

- GitHub 仓库：`https://github.com/zhipeng-yu/elm-lite-platform`
- 当前稳定分支：`main`
- 当前开发日：D1。A 板块脚手架已在功能分支完成，尚未合并到 `main`。
- 项目启动日期：2026-09-02。
- 三名成员均已加入仓库；龙、梁尚未完成首次克隆。
- 本次 D1 开发基于 `6b5651e Merge pull request #1 from zhipeng-yu/docs/team-collaboration-guide`。

## 成员和固定板块

| 成员 | 角色 | 固定负责板块 |
| --- | --- | --- |
| 余 | A：后端架构与 TDD | 后端公共模块、用户、鉴权、地址、订单、测试规范、README 与 handoff 汇总 |
| 梁 | B：数据库与后端业务 | 数据库迁移、商家、店铺、分类、商品、购物车及各自测试 |
| 龙 | C：前端与验收 | Vue、Router、Axios、页面、联调、验收与报告材料 |

每人只修改自己当天负责的板块。需要改动其他成员或共享文件时，先在群里说明，由对应负责人修改或审核。前端在后端接口尚未合并时按照已确认的接口契约和模拟数据开发，不直接修改后端代码。

## 每日 09:00 合并和状态更新

1. 每名成员完成当天任务后，必须完成自测、push 并创建 PR，然后在群里发送 `任务已完成 + PR 链接 + 测试结果`。
2. 余是本文件的唯一维护人。收到完成消息后，由余把对应任务从 `⬜ 待完成` 改为 `✅ 已完成（PR #编号）`；其他成员不要在功能分支中修改本文件。
3. 每天 08:50 前准备好昨天的 PR。普通组员的 PR 必须已有至少一名队友批准，相关测试必须通过。
4. 每天 09:00 按公共基础、后端业务、前端与文档的顺序合并昨天已完成的 PR。未完成、未审核或测试失败的 PR 不强行合并，任务继续保持待完成并注明原因。
5. 合并完成后，余从最新 `main` 创建文档分支，更新本文件的完成状态、PR 编号、当前开发日和阻塞，再通过 PR 合并。
6. 三人随后执行 `git switch main` 和 `git pull --ff-only`。拉取完成后，以本文件显示的当前开发日和任务状态为准创建当天分支。
7. 后端任务只有在测试提交早于实现提交、测试实际通过且 PR 已合并后，才能标记为已完成。没有运行测试不得填写通过。

任务状态只使用以下两种，便于快速识别：

- `⬜ 待完成`：尚未合并到 `main`，包括正在开发、等待审核或存在阻塞。
- `✅ 已完成（PR #编号）`：已经验证并合并到 `main`。

## 八个开发日任务看板

项目开发任务按 D1—D8 安排，其中原 D2—D15 的十四天工作压缩为 D2—D8 七个开发日。每个开发日开始前先完成 09:00 合并；D1 没有前一天代码需要合并。老师公布需求变更后，只替换 D6 的具体变更内容，不改变三人的固定板块，也不省略红—绿测试和回归证据。

### 并行开发边界

| 板块 | 可修改范围 | 当天不得直接修改 |
| --- | --- | --- |
| 余（A） | 后端公共配置，以及用户、鉴权、地址、订单对应的 Controller、Service、Mapper、Entity、DTO 和测试；根目录文档汇总 | B 的业务类、数据库迁移、`frontend/` |
| 梁（B） | `backend/src/main/resources/db/migration/`、测试数据，以及商家、店铺、分类、商品、购物车对应的后端类和测试 | A 的业务类、后端公共配置、`frontend/`、根目录文档 |
| 龙（C） | `frontend/` 下的页面、路由、请求封装、组件和前端测试 | `backend/`、数据库迁移、根目录文档 |

同名共享文件只允许表中负责人修改。跨板块需求先通过 Issue 或 PR 评论约定接口和字段，由文件负责人落地；不得为了赶进度直接改队友文件。数据库迁移编号统一由梁分配，根目录 README、handoff 和验收汇总统一由余修改。每天最多为当天任务创建一个功能 PR；后端 PR 仍必须保留测试提交早于实现提交的顺序。

| 开发日 | 余（A） | 梁（B） | 龙（C） |
| --- | --- | --- | --- |
| D1<br>日期：2026-09-02 | ⬜ 待完成：建立 Spring Boot 后端脚手架、分层目录、测试基础配置和依赖基线（本地已完成并通过测试，待 PR 合并） | ⬜ 待完成：设计 ER 图，建立 V1 数据库迁移、初始化 SQL 和字段说明 | ⬜ 待完成：建立 Vue 3 前端脚手架、Router 和页面目录，不修改后端文件 |
| D2<br>日期：待填写 | ⬜ 待完成：冻结 `/api/v1`、统一响应和异常契约；按红—绿流程实现后端公共规范 | ⬜ 待完成：基于 D1 的 V1 迁移建立 Entity、Mapper 和可重复测试数据，字段与冻结契约对齐 | ⬜ 待完成：基于 D1 前端脚手架封装 Axios、认证头、统一错误处理和基础布局，使用模拟数据验证加载、空数据和错误状态 |
| D3<br>日期：待填写 | ⬜ 待完成：按红—绿流程完成用户注册、登录、鉴权和个人信息 | ⬜ 待完成：按红—绿流程完成商家注册及店铺开店、关店、临时闭店 | ⬜ 待完成：完成注册、登录、店铺列表和店铺详情页面，按冻结契约使用模拟数据 |
| D4<br>日期：待填写 | ⬜ 待完成：按红—绿流程完成收货地址增删改查，覆盖权限、参数和边界场景 | ⬜ 待完成：按红—绿流程完成分类、商品列表、详情、价格和库存 | ⬜ 待完成：完成分类、商品列表、商品详情和地址页面，并联调已合并接口 |
| D5<br>日期：待填写 | ⬜ 待完成：按红—绿流程完成订单创建、列表和状态查询，回归 A 板块 | ⬜ 待完成：按红—绿流程完成购物车增删改查和下单所需库存校验，回归 B 板块 | ⬜ 待完成：完成购物车、下单和订单列表页面，跑通注册至订单查询的第一轮全流程 |
| D6<br>日期：待填写 | ⬜ 待完成：分析需求变更对 A 板块的影响，先提交失败测试，再完成最小实现和 A 板块回归 | ⬜ 待完成：分析需求变更对 B 板块及数据库的影响，先提交迁移与失败测试，再完成最小实现和 B 板块回归 | ⬜ 待完成：更新受影响页面、接口适配和验收场景，只修改 C 板块 |
| D7<br>日期：待填写 | ⬜ 待完成：执行 A 板块集成、权限、边界和覆盖率检查，只修复 A 问题并冻结后端公共契约 | ⬜ 待完成：执行 B 板块集成和边界回归，验证迁移可重复执行及全新数据库初始化，只修复 B 问题 | ⬜ 待完成：完成变更后的端到端联调和前端冒烟测试，补齐加载、空数据、校验和错误提示，按负责人登记问题 |
| D8<br>日期：待填写 | ⬜ 待完成：运行完整后端测试和覆盖率检查，检查 REST/统一响应规范，汇总 README、API 和技术交付说明 | ⬜ 待完成：执行干净环境数据库部署验证，准备演示数据、最终 SQL、数据字典和数据库说明 | ⬜ 待完成：执行最终演示与交叉验收，整理 Postman/ApiFox 集合、测试报告、需求变更记录、验收报告、截图和答辩材料 |

未完成任务只顺延，不把新功能挤进后一天。D7 之后原则上冻结接口和数据库结构；交叉验收发现的问题仍由原板块负责人修复，并保留对应测试或验收证据。剩余课程日作为风险缓冲，用于老师临时安排、外部交叉验收和未通过项返工，不预先增加可选模块。

## 已确认决定

- 2026 年 `.md` 总要求是主要需求和评分依据，四份 2021 年 PDF 仅作参考。
- JDBC、Servlet 不需要作为独立阶段成果提交。
- 默认只开发一套 Vue 3 + Spring Boot 前后端分离项目。
- 余负责 A，梁负责 B，龙负责 C。
- 使用 TDD；后端测试提交必须早于对应实现提交。
- 普通组员的 PR 至少需要一人审核；仓库所有者仍创建 PR，但可以使用管理员 bypass。
- 基础范围包含用户、商家、店铺、商品、购物车、地址和订单；不默认加入真实支付或管理员后台。
- D1 后端基线使用 JDK 21、Spring Boot 3.5.16、Maven 3.9.16、MyBatis-Plus 3.5.17 和 JaCoCo 0.8.15。
- 原 D2—D15 工作压缩为 D2—D8 七个开发日；不删减 TDD、需求变更回归、交叉验收和最终交付证据。

## 项目启动后确认

- [x] 后端 JDK、Spring Boot、Maven、MyBatis-Plus 和 JaCoCo 版本。
- [ ] Node.js 和 MySQL 工具的具体版本。
- [x] 后端目录 `backend/`、包名 `com.elmlite.platform`、数据库名 `elm_lite` 和端口 `8080`。
- [ ] 第一周接口清单、字段契约和验收标准。
- [ ] 登录鉴权方案。
- [x] D1 日期 `2026-09-02` 和分支 `feature/zp-backend-scaffold`。
- [ ] D1 Issue 编号。

## 风险与阻塞

- 余的 D1 本地实现和测试已完成，只有 PR 合并后才能在看板标记为已完成。
- GitHub 连接器无 Issue 写权限且本机 `gh` 未登录，D1 Issue 和 PR 需要余在网页创建。
- 龙、梁尚未完成首次克隆；开始 D1 前需按 `CONTRIBUTING.md` 完成环境检查和克隆。
- 如果一天内必须改同一个共享文件，由该文件负责人统一修改，其他成员通过 Issue 或 PR 评论提供内容。

## AI 使用记录

| 日期 | 用途 | 涉及文件 | 人工复核人 |
| --- | --- | --- | --- |
| 2026-09-01 | 汇总课程要求并建立团队、Git 与 AI 协作规则 | `readme.md`、`AGENTS.md`、`handoff.md` | 待填写 |
| 2026-09-01 | 为 GitHub 新手补充可逐步执行的 PowerShell、VS Code、PR 和冲突处理流程 | `readme.md`、`handoff.md` | 待填写 |
| 2026-09-01 | 将稳定成员协作规则迁移到 CONTRIBUTING，并同步 README、AI 规则和交接记录 | `readme.md`、`CONTRIBUTING.md`、`AGENTS.md`、`handoff.md` | 待填写 |
| 2026-09-01 | 修复 README 被旧编辑器内容覆盖，恢复精简项目入口 | `readme.md`、`handoff.md` | 待填写 |
| 2026-09-01 | 将 handoff 重构为三人每日任务与完成状态看板，并落实 09:00 合并和错峰修改规则 | `handoff.md`、`CONTRIBUTING.md`、`readme.md` | 待填写 |
| 2026-09-02 | 辅助建立 D1 Spring Boot 脚手架、红—绿测试基线、Maven Wrapper 和启动说明 | `backend/`、`.gitignore`、`readme.md`、`handoff.md` | 待余复核 |
| 2026-09-02 | 将原 D2—D15 任务压缩为 D2—D8，并按模块和文件所有权划分三人并行边界 | `handoff.md` | 待余复核 |
| 2026-09-02 | 纠正成员 B、C 的姓名映射，统一为梁负责 B、龙负责 C | `handoff.md` | 待余复核 |
