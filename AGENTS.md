# AI 协作规则

本文件适用于整个仓库。任何 AI 在分析、生成或修改项目内容前，都必须先阅读本文件、`readme.md`、`CONTRIBUTING.md`、`handoff.md` 以及与任务直接相关的课程文档。

## 1. 需求优先级

1. 当前用户或小组成员在本次任务中的明确要求。
2. 本文件中的团队约束。
3. `Instruction Manual/26271学期-软件工程综合实践.md`。
4. `handoff.md` 中已经确认的决定。
5. 四份旧 PDF，仅作业务和界面参考。

发现冲突或会明显改变范围的缺失决定时，停止扩展并请小组确认。不得擅自把管理员后台、真实支付、优惠券、配送算法等可选功能加入基础范围。

## 2. TDD 是硬性流程

每个后端接口和核心业务方法必须按以下顺序开发：

1. 明确成功、失败和边界行为。
2. 先编写测试并确认测试会因功能尚未实现而失败。
3. 单独提交测试，提交信息使用 `test(scope): ...`。
4. 编写满足测试的最小实现。
5. 运行相关测试和必要的回归测试。
6. 单独提交实现，提交信息使用 `feat(scope): ...` 或 `fix(scope): ...`。

AI 不得一次性生成整套业务模块及其全部测试。AI 可以提供测试思路、样板和小范围实现，但学生必须确认需求、编写或审阅测试，并能解释最终代码。

不得伪造测试结果、覆盖率、日志或 TDD 顺序。没有实际运行就明确写“未运行”。

## 3. 架构约束

后端：

- 使用 Controller、Service、Mapper/Dao、Entity 分层。
- Controller 只负责接收请求、参数校验和响应，不写复杂业务逻辑。
- 业务规则集中在 Service，事务边界放在 Service。
- 数据访问只放在 Mapper/Dao。
- 使用全局异常处理和统一 `code/msg/data` 响应。
- REST 接口统一使用 `/api/v1`；URI 优先使用资源名词。
- 取消订单等明确业务动作允许使用 `/orders/{id}/cancel`。
- 数据库结构变化使用版本化 SQL，禁止只手工修改本地数据库。

前端：

- 使用 Vue 3 组件和 Vue Router 管理页面。
- Axios 请求、认证头和错误处理统一封装。
- 公共组件只有在至少两处实际复用时才抽取。
- 页面必须提供基本的加载、空数据、校验和错误提示。

通用：

- 优先最小、清晰、可解释的实现，不为未知需求提前设计框架。
- 未经小组确认不新增依赖、不切换技术栈、不大范围重构。
- 不修改 `Instruction Manual/` 中的原始课程文件。
- 不读取、提交或输出密码、令牌、密钥和个人隐私信息。

## 4. Git 约束

- 不直接向 `main` 开发或强制推送。
- 一个 Issue 对应一个短期分支和一个 PR。
- 开始任务前从最新 `main` 创建分支。
- 不修改与当前任务无关的文件，不覆盖其他成员尚未提交的更改。
- 普通组员的 PR 合并前至少一名队友审核，相关测试必须通过。
- 仓库所有者自己的 PR 可以使用管理员 bypass 跳过审核，但仍必须创建 PR 并保留记录。
- 不使用 squash 合并，保留测试提交早于实现提交的历史。
- 推荐提交前缀：`test`、`feat`、`fix`、`refactor`、`docs`、`chore`。

## 5. AI 使用边界与记录

允许 AI 辅助：

- 项目脚手架、DTO、Entity、基础 Mapper 和页面模板。
- 测试样板、接口文档初稿、重复代码和格式整理。
- 编译错误、依赖问题、异常堆栈和 Bug 定位。
- REST、测试场景和需求变更影响的建议。

必须由小组决定和理解：

- 接口划分、数据库核心模型和架构方案。
- 核心业务规则及需求变更方案。
- 测试断言、测试结果判断和交叉验收结论。

每次 AI 产生实质修改后，在 PR 描述或 `handoff.md` 的“AI 使用记录”中写明：使用目的、涉及文件、人工复核人。不得把 AI 输出未经理解直接提交。

## 6. 完成任务时

AI 的交付说明必须包含：

- 修改了什么。
- 实际执行了哪些测试或检查及其结果。
- 未完成事项、风险和下一步。

完成一次有实质变化的工作后更新 `handoff.md`，但不要把长期规则复制进去。
